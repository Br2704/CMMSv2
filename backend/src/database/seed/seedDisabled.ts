// Intentional no-op: protects startup flows from creating seed users automatically.
// Use `npm run seed:manual` only when seed data is explicitly required.
console.log("Seeding skipped. Use `npm run seed:manual` to run seed data intentionally.");
